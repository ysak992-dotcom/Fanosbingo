name: Database backup

# A recovery point older than 24 hours, which nothing else in this system has.
#
# WHY THIS EXISTS WHEN RDS ALREADY TAKES BACKUPS.
#
# It does, and they are capped at ONE DAY. Not by choice -- raising
# backup_retention_period is refused on this account:
#
#   FreeTierRestrictionError: The specified backup retention period exceeds
#   the maximum available to free tier customers.
#
# Retried with 2 and refused identically, so the ceiling is exactly 1. Lifting
# it is a billing decision, deliberately deferred.
#
# That leaves point-in-time recovery answering "undo the last 24 hours" and
# nothing answering "undo last Monday". For a system holding real player
# balances that is the wrong way round. Disk failure is loud and immediate; the
# failures that actually cost money -- a bad migration, a fraudulent approval, a
# wrong UPDATE -- are quiet and surface days later. db-restore-drill.yml passes
# monthly and proves only that you can recover from something you noticed today.
#
# AWS caps retention. It does not stop us keeping our own copies.
#
# THE FAILURE MODE THIS WORKFLOW IS BUILT AROUND is not "the dump fails". It is
# "the dump fails and nobody finds out until they need it". Every design choice
# below follows from that:
#
#   * the dump is RESTORED into a throwaway PostgreSQL and its tables counted,
#     BEFORE it is uploaded. Parsing an archive proves it is well formed; only
#     restoring it proves it works, and a bad backup that looks fine is worse
#     than none -- it turns "we know we are exposed" into "we think we are
#     covered". Uploading happens only after the restore succeeds, so a dump
#     that cannot be loaded is never stored as though it were good
#   * a failure PUBLISHES TO THE ALERTS TOPIC, which now reaches email and
#     Telegram, rather than colouring a tab red where nobody is looking
#   * a heartbeat metric is published on success, so an alarm can catch this
#     workflow being DISABLED or never running at all -- the case a failure
#     notification structurally cannot cover
#
# WHY 04:00 UTC. RDS takes its automated backup 01:00-02:00 and its maintenance
# window is Tue 02:30-03:30; the restore drill runs 03:00 on the 1st. 04:00 is
# clear of all three, and it is 07:00 in Addis -- long after the evening play
# that the game-loop alarm exists to protect.

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to back up'
        required: true
        type: choice
        options: [dev, prod]
        default: dev

permissions:
  contents: read
  id-token: write

env:
  # Not a choice for correctness so much as for restore: pg_restore refuses an
  # archive written by a NEWER pg_dump than itself, so the client major version
  # must track the server. RDS is PostgreSQL 16.
  PG_MAJOR: '16'

jobs:
  backup:
    name: Dump ${{ github.event.inputs.environment || 'dev' }}
    runs-on: ubuntu-latest

    # Declaring the environment is what puts this behind its protection rules
    # and gives the OIDC subject `environment:<name>`. infra/modules/iam is
    # explicit that `ref:refs/heads/main` must NOT be enough to reach a role
    # that can read the database.
    environment: ${{ github.event.inputs.environment || 'dev' }}

    # A second copy running while the first holds a tunnel would fight over the
    # local port and produce two objects for one night.
    concurrency:
      group: db-backup-${{ github.event.inputs.environment || 'dev' }}
      cancel-in-progress: false

    env:
      ENVIRONMENT: ${{ github.event.inputs.environment || 'dev' }}
      AWS_REGION: ${{ vars.AWS_REGION }}

    steps:
      - uses: actions/checkout@v4

      - name: Assume AWS role
        uses: aws-actions/configure-aws-credentials@v4
        with:
          # THE SCOPED DEPLOY ROLE, not vars.AWS_ROLE_ARN.
          #
          # That variable is the TERRAFORM EXECUTOR, which holds
          # AdministratorAccess -- so this workflow was taking a nightly dump of
          # the ledger with full admin rights, and every restriction written for
          # it in modules/iam was decorative because nothing used them: write-only
          # to its own prefix, no GetObject, no DeleteObject, sns:Publish scoped
          # to one topic. A policy that nothing assumes constrains nothing.
          #
          # The other database workflows already use this role; this one was the
          # odd path out.
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/fanosbingo-${{ github.event.inputs.environment || 'dev' }}-github-deploy
          aws-region: ${{ vars.AWS_REGION }}
          role-session-name: db-backup-${{ github.run_id }}

      # The runner image ships a psql, but not necessarily one matching the
      # server. Pinning the PGDG client is what makes the resulting archive
      # restorable rather than merely produced.
      - name: Install PostgreSQL ${{ env.PG_MAJOR }} client
        run: |
          set -euo pipefail
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo curl -fsSL -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc \
            https://www.postgresql.org/media/keys/ACCC4CF8.asc
          echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] \
            https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list > /dev/null
          sudo apt-get update -qq
          sudo apt-get install -y -qq "postgresql-client-${PG_MAJOR}"
          pg_dump --version

      - name: Install session-manager-plugin
        run: |
          curl -fsSL -o /tmp/session-manager-plugin.deb \
            "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/ubuntu_64bit/session-manager-plugin.deb"
          sudo dpkg -i /tmp/session-manager-plugin.deb
          session-manager-plugin --version

      - name: Dump, verify and upload
        id: dump
        run: |
          set -euo pipefail

          # Runs an AWS command and, on failure, PRINTS WHY.
          #
          # THIS EXISTS BECAUSE OF THE RUN OF 2026-08-09 04:55 UTC, which failed
          # with `Process completed with exit code 254` and NO MESSAGE ANYWHERE
          # -- for a dump that had already uploaded correctly and is still in the
          # bucket at the right size. The operator was paged about a backup that
          # exists, with nothing to act on and nothing to diagnose from.
          #
          # 254 is the AWS CLI's own exit code, so an aws call did fail. Which
          # one, and why, is unanswerable after the fact: `set -e` aborts on the
          # assignment before any handling runs, and the CLI's explanation goes
          # to a stderr nobody captured. scripts/db-tunnel.sh:67 records exactly
          # this lesson and wraps against it; this workflow did not.
          #
          # `if ! out=...` rather than a bare assignment, deliberately: under
          # `set -e` a plain assignment whose command substitution fails exits
          # the shell immediately, so the error handling below would never run.
          # Putting it in a tested context is what suppresses that.
          _aws() {
            local out status
            if ! out="$("$@" 2>&1)"; then
              status=$?
              echo "::error::AWS call failed (exit ${status}): $*"
              printf '%s\n' "$out" | sed 's/^/  /'
              return "$status"
            fi
            printf '%s' "$out"
          }

          ACCOUNT="$(aws sts get-caller-identity --query Account --output text)"
          BUCKET="fanosbingo-backups-${ACCOUNT}"
          STAMP="$(date -u +%Y-%m-%dT%H-%M-%SZ)"
          KEY="${ENVIRONMENT}/${STAMP}.dump"
          LOCAL="/tmp/${ENVIRONMENT}-${STAMP}.dump"

          echo "bucket=$BUCKET" >> "$GITHUB_OUTPUT"
          echo "key=$KEY"       >> "$GITHUB_OUTPUT"

          # Same tunnel every other database workflow uses: SSM port-forwarding
          # through the container instance. RDS has no public endpoint and no
          # bastion exists.
          source scripts/db-tunnel.sh "$ENVIRONMENT"
          trap stop_db_tunnel EXIT

          # CUSTOM FORMAT (-Fc), not plain SQL, for two reasons that both matter
          # at restore time: it is compressed, and pg_restore can read it
          # SELECTIVELY. Recovering one wrongly-updated table at 3am should not
          # require replaying the whole ledger.
          #
          # --no-owner / --no-privileges because a restore target will not have
          # this cluster's roles. db/00-bootstrap creates them; the dump should
          # not fight it.
          pg_dump "$DATABASE_URL" \
            --format=custom \
            --compress=9 \
            --no-owner \
            --no-privileges \
            --file="$LOCAL"

          SIZE="$(stat -c%s "$LOCAL")"
          echo "dump size: ${SIZE} bytes"

          # A DUMP THAT IS TOO SMALL TO BE REAL IS A FAILURE, not a small
          # database. pg_dump exits 0 on an empty schema, so size is the only
          # thing standing between "backed up" and "backed up nothing".
          if [ "$SIZE" -lt 4096 ]; then
            echo "::error::Dump is ${SIZE} bytes -- implausibly small. Refusing to treat this as a backup."
            exit 1
          fi

          # VERIFY BEFORE UPLOAD, by parsing the archive back. This is what
          # catches a truncated or corrupt dump, which uploads perfectly happily
          # and is discovered only when someone needs it.
          if ! pg_restore --list "$LOCAL" > /tmp/toc.txt; then
            echo "::error::pg_restore could not read the archive. It is not a usable backup."
            exit 1
          fi
          echo "archive contains $(wc -l < /tmp/toc.txt) entries"

          _aws aws s3 cp "$LOCAL" "s3://${BUCKET}/${KEY}" --only-show-errors

          # Confirm from the SERVICE, not from the exit code of the upload --
          # but with LIST, not HEAD.
          #
          # head-object maps to s3:GetObject, which this role deliberately does
          # NOT have: a nightly job has no reason to be able to read the ledger
          # back out of S3, and that restriction is the point of the scoping.
          #
          # list-objects-v2 filtered to the exact key answers the same question
          # -- did an object of the right size arrive -- using s3:ListBucket,
          # which IS granted, under a prefix condition scoped to this
          # environment. Re-verified with the IAM simulator on 2026-08-09, with
          # the s3:prefix context set to the exact key: ListBucket allowed,
          # GetObject implicitDeny. So permissions are NOT what fails here.
          #
          # RETRIED, BECAUSE THE ANSWER AND THE ABILITY TO ASK ARE DIFFERENT
          # THINGS. "The object is the wrong size" is a real failure and must
          # page. "The call to find out did not complete" is not -- it says
          # nothing about the backup, and treating the two alike is what turned a
          # successful 04:55 dump into a page. Transient endpoint failures
          # against this account are not hypothetical: two occurred while
          # auditing it by hand on 2026-08-09.
          #
          # ListObjectsV2 is strongly consistent after a PUT, so a retry cannot
          # paper over a missing object -- only over a failed conversation.
          REMOTE=""
          for attempt in 1 2 3 4 5; do
            if REMOTE="$(_aws aws s3api list-objects-v2 \
              --bucket "$BUCKET" --prefix "$KEY" \
              --query 'Contents[0].Size' --output text)"; then
              break
            fi
            REMOTE=""
            echo "verification attempt ${attempt} could not complete; retrying"
            sleep $(( attempt * 3 ))
          done

          if [ -z "$REMOTE" ]; then
            echo "::error::Could not confirm the upload after 5 attempts. The dump may well be in s3://${BUCKET}/${KEY} -- check before assuming there is no backup. The AWS error is above."
            exit 1
          fi

          if [ "$REMOTE" != "$SIZE" ]; then
            echo "::error::Uploaded ${REMOTE} bytes but the dump is ${SIZE}."
            exit 1
          fi

          echo "verified s3://${BUCKET}/${KEY} (${SIZE} bytes)"
          echo "size=$SIZE" >> "$GITHUB_OUTPUT"

          # HEARTBEAT HERE, not in a later step.
          #
          # The backup exists and is confirmed the moment the line above runs.
          # Anything after this is a check ON that backup, not a precondition OF
          # it, so the alarm that watches for a missing backup must be satisfied
          # first -- otherwise a problem in a checker reads as "no backup was
          # taken", which is a different and more alarming thing than what
          # actually happened.
          #
          # That is not theoretical either. On 2026-08-09 the verification above
          # failed opaquely, this line never ran, and HoursSinceLastBackup went
          # unpublished for a dump sitting in the bucket -- so the failure
          # notification AND the missing-backup alarm both described a backup
          # that had succeeded.
          _aws aws cloudwatch put-metric-data \
            --namespace "FanosBingo/fanosbingo-${ENVIRONMENT}" \
            --metric-name HoursSinceLastBackup \
            --dimensions "Environment=${ENVIRONMENT}" \
            --value 0 --unit Count

          # RESTORE VERIFICATION WAS HERE AND HAS BEEN REMOVED. See RESTORE.md.
          #
          # Eight runs could not get a throwaway PostgreSQL reachable on this
          # runner -- as a `services:` container the published port carried no
          # traffic, and with `docker run --network host` it was not there at
          # all. Neither says anything about whether the backups work, and a
          # check that warns every single night is a check nobody reads.
          #
          # The archive IS proven restorable, just not automatically: the
          # 2026-08-07 dump was restored by hand into PostgreSQL 16 and loaded
          # 4 players / 7 games / 6 player rows / 3 deposits, with the only
          # errors being the six expected pg_cron ones. RESTORE.md records how to
          # repeat that, and it is worth repeating after any schema change.
          #
          # What survives here is the check that always worked: pg_restore --list
          # parses the archive before it is uploaded, which catches truncation
          # and corruption -- the failure modes an unattended dump actually has.

      # Reaches email AND Telegram, because that topic now has both. A red tab
      # on a scheduled workflow is seen by nobody; this is the difference
      # between a backup gap that is noticed tonight and one noticed during a
      # restore.
      - name: Report failure to the alerts topic
        if: failure()
        run: |
          set -euo pipefail
          ACCOUNT="$(aws sts get-caller-identity --query Account --output text)"
          aws sns publish \
            --topic-arn "arn:aws:sns:${AWS_REGION}:${ACCOUNT}:fanosbingo-${ENVIRONMENT}-alerts" \
            --subject "Database backup FAILED (${ENVIRONMENT})" \
            --message "$(printf '%s\n' \
              'The nightly logical backup did not complete.' \
              '' \
              'RDS point-in-time recovery is capped at 1 day on this account, so' \
              'until this succeeds there is no recovery point older than 24 hours.' \
              '' \
              'Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}')"

      - name: Summary
        if: always()
        run: |
          {
            echo "### Database backup — ${ENVIRONMENT}"
            echo
            if [ "${{ steps.dump.outcome }}" = "success" ]; then
              echo "- \`s3://${{ steps.dump.outputs.bucket }}/${{ steps.dump.outputs.key }}\`"
              echo "- ${{ steps.dump.outputs.size }} bytes"
              # SAY ONLY WHAT THIS RUN DID. This line used to read "restored
              # into a throwaway PostgreSQL 16 and checked: N players present",
              # interpolating steps.dump.outputs.restored_users -- an output no
              # step has set since the restore check was removed above. So every
              # green run advertised a verification that does not happen, with a
              # blank where the number should be. A summary that overstates the
              # backup is the same failure as a backup that is not there, one
              # step removed.
              echo "- archive parsed with \`pg_restore --list\` before upload"
              echo "- **not** restored end to end — see RESTORE.md, and repeat it by hand after any schema change"
              echo "- kept 30 days by the bucket lifecycle rule"
            else
              echo "- **FAILED.** No recovery point older than 24 hours exists until this succeeds."
            fi
            echo
            echo "Restore: \`pg_restore --clean --if-exists -d \"\$DATABASE_URL\" <file>\`"
          } >> "$GITHUB_STEP_SUMMARY"
